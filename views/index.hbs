<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light px-3">
        <a class="navbar-brand" href="/">eShop</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <!--<a class="nav-link" href="/dashboard">Panel Administracyjny</a>-->
                </li>
            </ul>
        </div>
        <div>
            <div class="dropdown">
                {{#if authStatus}}
                    <a class="btn btn-primary" href="/cart">Koszyk</a>
                    <a href="/logout" class="btn btn-primary">Wyloguj</a>
                {{else}}
                    <a href="/login" class="btn btn-primary">Zaloguj</a>
                    <a href="/register" class="btn btn-primary">Rejestracja</a>
                {{/if}}
            </div>
        </div>
    </nav>
    <div class="my-3">
        <div class="row">
        {{#each productData}}
            <div class="card" style="width: 20vw; margin: 2.5vw;">
                <div class="card-body">
                    <img class="card-img-top mb-3" src="{{this.image_url}}" alt="Card image cap">
                    <h5 class="card-title">{{this.name}}</h5>
                    <p class="card-text">{{this.description}}</p>
                    <a class="btn btn-primary" href="{{setDetailUrl this.id_p}}">Go somewhere</a>
                </div>
            </div>
        {{/each}}
        </div>
    </div>
</body>
<script>
    function goToDetails(id) {
        console.log(id)
    }
</script>
</html>